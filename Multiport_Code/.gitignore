# pixi environments
.pixi/*
!.pixi/config.toml
# Data Folder
Data/
# Claude
CLAUDE.md

# Pycache — matches at any depth (Multiport_Code/ and gui/)
__pycache__/

# Local configuration: the RSpace API key and the rig's beamer, pump and camera
# calibrations. Machine-specific and secret, so they stay out of the repo.
config/config.json
config/beamer_calibration.json
config/pump_calibration.json
config/camera_calibration.json

# Unsaved RSpace entry drafts
Autosaved/
